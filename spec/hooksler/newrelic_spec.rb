require 'spec_helper'

describe Hooksler::Newrelic do
  it 'has a version number' do
    expect(Hooksler::Newrelic::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
